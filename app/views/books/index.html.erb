<h2>Daftar Buku</h2>
  <%= link_to 'Add New Book', new_book_path %> 
  <%
=begin%>
 <%= link_to  "Add New Book", {controller:'books', action:'new'} %> 
<%
=end%>

<table>
    <tr>
        <th>Title</th>
        <th>Pages</th>
        <th>Description</th>
        <th>Action</th>
    </tr>
    <% @books.each do |book| %>
    <tr>
        <td><%= book.title %></td>
        <td><%= book.page %></td>
        <td><%= book.description %></td>        
        <td><%= link_to 'SHOW', book_path(book) %> | 
          <%= link_to 'EDIT', edit_book_path(book)%> | 
          <%= link_to  "DELETE", {controller:'books', action:'destroy', id: book.id}, method: :delete %>
        </td>
    </tr>
    <% end %>
</table>
